"""MuJoCo simulation placeholder."""


def main() -> None:
    """Explain the expected simulation asset path."""
    # Linkerbot MJCF assets are not bundled yet, so the example is explicit.
    # Place validated meshes and kinematics in shared resources before use.
    print("Place MJCF assets under shared simulation resources before running this demo.")


# The guard avoids printing during documentation imports or test discovery.
if __name__ == "__main__":
    main()
