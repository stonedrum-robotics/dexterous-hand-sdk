# Documentation Pointer

Canonical documentation lives in the separate `docs` repository and is published through MkDocs Material.

This directory can hold generated API docs or a submodule pointer once the repositories are created.
